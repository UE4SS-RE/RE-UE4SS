if (DEFINED CMAKE_CXX_COMPILER_ID AND NOT ${CMAKE_CXX_COMPILER_ID} STREQUAL "")
    if (${CMAKE_CXX_COMPILER_ID} STREQUAL Clang)
        set(COMPILER_NAME Clang)
    elseif (${CMAKE_CXX_COMPILER_ID} STREQUAL MSVC)
        set(COMPILER_NAME MSVC)
    else ()
        set(COMPILER_NAME Unknown)
    endif ()
else ()
    set(COMPILER_NAME Unknown)
endif ()

if (DEFINED CMAKE_CXX_SIMULATE_ID AND NOT ${CMAKE_CXX_SIMULATE_ID} STREQUAL "")
    if (${CMAKE_CXX_SIMULATE_ID} STREQUAL Clang)
        set(COMPILER_SIMULATION_NAME Clang)
    elseif (${CMAKE_CXX_SIMULATE_ID} STREQUAL MSVC)
        set(COMPILER_SIMULATION_NAME MSVC)
    else ()
        set(COMPILER_SIMULATION_NAME Unknown)
    endif ()
else ()
    set(COMPILER_SIMULATION_NAME Unknown)
endif ()

if (DEFINED CMAKE_CXX_COMPILER_FRONTEND_VARIANT AND NOT ${CMAKE_CXX_COMPILER_FRONTEND_VARIANT} STREQUAL "")
    if (${CMAKE_CXX_COMPILER_FRONTEND_VARIANT} STREQUAL Clang)
        set(COMPILER_FRONTEND_NAME Clang)
    elseif (${CMAKE_CXX_COMPILER_FRONTEND_VARIANT} STREQUAL MSVC)
        set(COMPILER_FRONTEND_NAME MSVC)
    elseif (${CMAKE_CXX_COMPILER_FRONTEND_VARIANT} STREQUAL GNU)
        set(COMPILER_FRONTEND_NAME GNU)
    else ()
        set(COMPILER_FRONTEND_NAME Unknown)
    endif ()
else ()
    set(COMPILER_FRONTEND_NAME Unknown)
endif ()